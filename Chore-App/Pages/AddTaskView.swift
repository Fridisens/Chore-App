//forms for adding task that you then can find at profile page
import SwiftUI

struct AddTaskView: View {
    var body: some View {
        VStack {
            Text("Add a New Task")
                .font(.largeTitle)
                .padding()
        }
    }
}
