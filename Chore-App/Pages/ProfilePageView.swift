import SwiftUI

//shows profile with avatar and added chores

struct ProfilePageView: View {
    var body: some View {
        VStack {
            Text("Profile")
                .font(.largeTitle)
                .padding()
            
            
            Text("Chores List")
                .font(.largeTitle)
                .padding()
        }
    }
}
